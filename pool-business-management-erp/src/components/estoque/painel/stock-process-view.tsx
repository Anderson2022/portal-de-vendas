"use client";
import {useState} from "react";import {ArrowLeftRight,ClipboardCheck} from "lucide-react";import {Button} from "@/components/ui/button";import {Card} from "@/components/ui";import type {ProductFormProps} from "../produto/product-types";import {StockTransferModal} from "../transferencia/stock-transfer-modal";import {InventoryModal} from "../inventario/inventory-modal";import type {StockOverviewData} from "./stock-overview-types";
export function StockProcessView({kind,s,productOptions}:{kind:"transferencias"|"inventarios";s:StockOverviewData;productOptions:Pick<ProductFormProps,"warehouses">}){const [open,setOpen]=useState(false),transfer=kind==="transferencias";return <><Card className="p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-3">{transfer?<ArrowLeftRight/>:<ClipboardCheck/>}<div><h2 className="text-lg font-extrabold">{transfer?"Transferências":"Inventários"}</h2><p className="text-sm text-ink-400">{transfer?"Movimentações entre depósitos.":"Contagem e conferência do estoque."}</p></div></div><Button variant="primary" onClick={()=>setOpen(true)}>{transfer?"Nova transferência":"Novo inventário"}</Button></div></Card>{open&&transfer&&<StockTransferModal products={s.rows.map(p=>({id:p.id,name:p.name,sku:p.sku,unitId:String(p.values.unitId||""),unitName:p.unit,warehouseId:String(p.values.warehouseId||""),available:p.available}))} warehouses={productOptions.warehouses} onClose={()=>setOpen(false)}/>} {open&&!transfer&&<InventoryModal products={s.rows.map(p=>({id:p.id,name:p.name}))} warehouses={productOptions.warehouses} onClose={()=>setOpen(false)}/>}</>}
